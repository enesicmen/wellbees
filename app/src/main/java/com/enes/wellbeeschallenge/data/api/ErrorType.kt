package com.enes.wellbeeschallenge.data.api

enum class ErrorType {
    HTTP,
    GENERIC,
    UNEXPECTED_RESPONSE,
    UNDEFINED,
    CONNECTION,
    AUTHENTICATION,
    DB_ITEM_NOT_FOUND,
    USECASE
}
